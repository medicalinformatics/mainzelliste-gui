import { InvalidConsentPeriodDirective } from './invalid-consent-period.directive';

describe('InvalidConsentPeriodDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidConsentPeriodDirective();
    expect(directive).toBeTruthy();
  });
});
