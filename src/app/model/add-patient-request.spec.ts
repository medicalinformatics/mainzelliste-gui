import { AddPatientRequest } from './add-patient-request';

describe('AddPatientRequest', () => {
  it('should create an instance', () => {
    expect(new AddPatientRequest()).toBeTruthy();
  });
});
