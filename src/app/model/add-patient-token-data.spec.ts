import { AddPatientTokenData } from './add-patient-token-data';

describe('AddPatientTokenData', () => {
  it('should create an instance', () => {
    expect(new AddPatientTokenData()).toBeTruthy();
  });
});
