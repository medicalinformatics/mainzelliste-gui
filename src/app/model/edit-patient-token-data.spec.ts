import { EditPatientTokenData } from './edit-patient-token-data';

describe('EditPatientTokenData', () => {
  it('should create an instance', () => {
    expect(new EditPatientTokenData()).toBeTruthy();
  });
});
