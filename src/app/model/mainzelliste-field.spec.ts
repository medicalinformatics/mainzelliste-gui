import { MainzellisteField } from './mainzelliste-field';

describe('MainzellisteField', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new MainzellisteField()).toBeTruthy();
  });
});
