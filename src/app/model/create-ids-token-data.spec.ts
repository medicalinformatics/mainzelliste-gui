import { CreateIdsTokenData } from './create-ids-token-data';

describe('CreateIdsTokenData', () => {
  it('should create an instance', () => {
    expect(new CreateIdsTokenData()).toBeTruthy();
  });
});
