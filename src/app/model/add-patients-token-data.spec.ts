import { AddPatientsTokenData } from './add-patients-token-data';

describe('AddPatientsTokenData', () => {
  it('should create an instance', () => {
    expect(new AddPatientsTokenData()).toBeTruthy();
  });
});
