import { ReadPatientsTokenData } from './read-patients-token-data';

describe('ReadTokenData', () => {
  it('should create an instance', () => {
    expect(new ReadPatientsTokenData()).toBeTruthy();
  });
});
